<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fightcovid2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FightCovid2">
        <activity android:name=".MainActivity" />
        <activity android:name=".NewsDetailActivity" />
        <activity
            android:name=".CountryListActivity"
            android:noHistory="true" />
        <activity android:name=".HealthTips" />
        <activity android:name=".KnowAboutVaccine" />
        <activity android:name=".About" />
        <activity android:name=".CovidNews" />
        <activity android:name=".VaccineAvailability" />
        <activity
            android:name=".SplashScreenActivity"
            android:noHistory="true"
            android:theme="@style/Theme.MaterialComponents.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>